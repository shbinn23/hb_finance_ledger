# 후잉 거래 마이그레이션 로그

## 개요

`report.fact_transactions` (PostgreSQL) → 후잉 API → `whooing.entries` 동기화

- **마이그레이션 스크립트**: `etl/migrate_whooing_entries.py`
- **실행 환경**: `/Users/shbinn/dev/hb_finance_pipeline/.venv/bin/python3`
- **최종 완료일**: 2026-05-10 ✅ (잔액조정 surgical fix 포함)

---

## 실행 방법

```bash
cd /Users/shbinn/dev/hb_finance_pipeline_v2

# 1단계: 기존 entries 전량 삭제 + API 0건 검증
PYTHONUNBUFFERED=1 python3 etl/migrate_whooing_entries.py --delete

# 2단계: 후잉 앱에서 0건 확인 후 업로드
PYTHONUNBUFFERED=1 python3 etl/migrate_whooing_entries.py --upload
```

> `PYTHONUNBUFFERED=1` 필수 — 없으면 tee 파이프 버퍼링으로 로그 미출력

---

## 재실행 시 필수 원칙

DB 데이터만 믿고 바로 실행하지 말 것. 아래 순서 준수:

1. **사용자 기준값 수령** — 월별 수입/지출 실제 데이터를 먼저 받아 기준으로 삼기
2. **계정 매핑 재확인** — DB acc_id ↔ 후잉 x-id 연결이 실제와 맞는지 확인
3. **특수 케이스 사전 확인** — 카드대금 결제, 캐시백, 이체 처리 방식 확인
4. **STEP 2.5 시뮬 검증** — 10개 목표 계좌 전부 일치 확인 후 업로드 진행
5. **후잉 앱 직접 검증** — `--delete` 완료 후 앱에서 0건 확인, `--upload` 완료 후 잔고 확인

---

## 거래 처리 규칙

| 거래유형 | 처리 | 후잉 entry |
|---------|------|-----------|
| 지출 | 포함 | l=지출계정(category), r=결제계정(account) |
| 수입 | 포함 | l=입금계정(account), r=수입계정(category) |
| 이체입금 | 포함 | l=입금계정(account), r=출금계정(category→ACC 폴백) |
| 이체출금 | **스킵** | 이체입금에서 양측 처리 |
| 차액수입 | 포함 | l=계좌, r=자본(x1) — 잔액수정(기초잔액 포함) |
| 차액지출 | 포함 | l=자본(x1), r=계좌 — 잔액수정(하향 조정) |

---

## 체크카드 → 은행 라우팅

체크카드는 즉시 은행 출금이므로 r_account를 연결 은행으로 대체:

| 체크카드 | 연결 은행 |
|---------|---------|
| x91 우체국개이득 | x28 우체국 |
| x93 국민나사카 | x3 국민은행 |
| x94 신한하이패스 | x3 국민은행 |
| x95 하나나사카 | x26 하나은행 |

---

## 스킵 계좌

| 계좌 | 이유 |
|------|------|
| x27 부산은행 | 앱에서 삭제됨 |
| x33 퇴직연금 | 앱에서 삭제됨 |

---

## 잔액조정 entries (2025-06-30)

**최종 적용 현황** (2026-05-10 surgical fix 반영):

| 계좌 | l | r | 금액 | 비고 |
|------|---|---|------|------|
| x3 국민은행 | capital(x1) | assets(x3) | +100,000 | ✅ 적용 중 |
| x46 BCGoat | capital(x1) | liabilities(x46) | +159,536 | ✅ 적용 중 |
| x22 우리SKT | capital(x1) | liabilities(x22) | +10,000 | ✅ 적용 중 |
| x45 하나MGS | capital(x1) | liabilities(x45) | +9,900 | ✅ 9,810→9,900 수정 (2026-05-10) |
| x51 롯데라이킷 | — | — | — | ❌ 삭제 (2026-05-10, 이중계산 제거) |

---

## 금액 기준

- **`net_amount`** 사용 (매입금액, 카드 캐시백 등 반영된 실제 순금액)
- `amount`(승인금액) 사용 시 잔액 불일치 발생

---

## API 주요 제약사항

| 항목 | 내용 |
|------|------|
| GET limit | 최대 100건/월 (공식앱은 5000) |
| POST batch | 최대 300건 |
| DELETE | **단건만 가능** — 복수 IDs URL → WAF 403 차단 |
| PUT accounts | **실질적 미지원** — HTTP 200이지만 내부 code=400 반환 |
| Rate limit | 분당 20회 이하 (`SLEEP_SEC = 3`) |

---

## 계정 매핑 특이사항

| 항목 | 내용 |
|------|------|
| x22 (우리SKT) | `open_date` 앱에서 직접 수정 필요 (API PUT 미지원) — 19760124로 설정 완료 |
| x96 (우리SKT구) | x22로 통합 완료. ACC_539af17776 → x22 매핑 |
| CAT_f9d2fdc15d | 캐시백 중복 → `CAT_d6323c2be9` 별칭으로 처리 |
| x1/x10/x21 | `open_date` 이미 20200101로 수정됨 |
| 수입/기타 카테고리 | `r.type != income`인 경우 → `x10`(기타수익) fallback |

---

## 마이그레이션 단계

```
STEP -1  x22 open_date 수정 시도 (API 미지원, 앱에서 직접 수정)
STEP 0   기존 후잉 entries 전량 삭제 (다중 패스, API 0건 검증 후 종료)
────────────────────────── --delete 종료, 사용자 후잉 앱 확인 ──────────────────────────
STEP 1   계정 매핑 구축 (x96→x22 DB 재매핑 포함, 66개)
STEP 2   거래 변환 + 체크카드 라우팅 + 잔액조정 6건 주입
STEP 2.5 잔고 시뮬레이션 검증 (10개 계좌 불일치 시 abort)
STEP 3   후잉 API 업로드 (배치 300건)
STEP 4   후잉 API 재조회 → 로컬 DB 동기화 (월별 분할)
```

---

## 처리 건수 (2026-05-10 최종)

| 거래유형 | 건수 |
|---------|------|
| 지출 | 1,174건 |
| 이체입금 | 124건 |
| 수입 | 57건 |
| 차액수입 | 38건 |
| 차액지출 | 6건 |
| 잔액조정 | 5건 → 4건 (x51 삭제) |
| **후잉 최종** | **1,403건** |
| 이체출금 (스킵) | 125건 |

---

## 주요 이슈 및 해결

### 1. DELETE WAF 차단
- **증상**: 복수 IDs URL → HTTP 403
- **해결**: 단건 DELETE + 다중 패스 루프

### 2. GET 100/month 한계로 삭제 불완전
- **증상**: 패스 1에서 일부만 조회되어 나머지 생존
- **해결**: API 0건 반환까지 패스 반복

### 3. 체크카드 라우팅 누락
- **증상**: 체크카드 계좌에 잔액 쌓임, 은행 잔고 부족
- **해결**: CHECK_CARD_TO_BANK 라우팅 추가

### 4. x22 open_date API PUT 미지원
- **증상**: HTTP 200 반환하지만 내부 code=400, 실제 변경 없음
- **해결**: 후잉 앱에서 직접 시작일 수정

### 5. 이전 수동 기초잔액 entry 잔류 (2024-02-24)
- **증상**: MONTHS 범위가 2025-06~이라 2024년 entry 사각지대
- **해결**: MONTHS를 2020년~으로 확장, 해당 entry 수동 삭제

### 6. stdout 버퍼링
- **증상**: tee 파이프 시 로그 미출력
- **해결**: PYTHONUNBUFFERED=1 환경변수 추가

### 7. 잔액조정 이중계산 (x45, x51)
- **증상**: 후잉 기초잔액 설정(2025-06-30)과 업로드한 잔액조정 entry가 중복 계산됨
  - x51: 후잉 239,589 / 목표 225,984 → +13,605 초과
  - x45: 후잉 940,820 / 목표 940,910 → -90 부족
- **해결**: `etl/fix_adj_entries.py` 실행 (전체 재마이그레이션 없이 surgical fix)
  - x51 잔액조정 entry 삭제 (entry_id=1418505)
  - x45 잔액조정 money 9,810 → 9,900으로 PUT (entry_id=1418504)

### 8. whooing.entries 동기화 불완전
- **증상**: STEP 4 월별 API limit=100 하드캡으로 941건만 동기화 (1,404건 중)
- **해결**: 주간 단위(7일) 분할 조회로 재동기화 → 1,403건 완전 동기화
  - 월별 조회로는 busy month에서 100건 초과분 누락됨
  - `etl/migrate_whooing_entries.py` STEP 4 개선 필요 (TODO)

---

## 목표 계좌 잔고 (2026-05-10 기준, 19개)

| 계좌 | x-id | 기대값 |
|------|------|--------|
| 국민은행 | x3 | 374,347 |
| 신한은행 | x24 | 2,835 |
| 우체국 | x28 | 20,115 |
| 우리Npay | x30 | 2,019,111 |
| 청년적금 | x32 | 15,400,000 |
| 새마을예금 | x34 | 25,000,000 |
| 네이버CMA | x35 | 2,450,310 |
| 아이오닉 | x38 | 14,070,000 |
| 보증금 | x39 | 30,000,000 |
| 우리SKT (카드) | x22 | 309,010 |
| 우리Olleh (카드) | x43 | 0 |
| 하나MGS (카드) | x45 | 940,910 |
| BCGoat (카드) | x46 | 7,881 |
| 신한레이디 (카드) | x50 | 524,525 |
| 롯데라이킷 (카드) | x51 | 225,984 |
| 농협플렉스 (카드) | x53 | 26,150 |
| 국민CJ (카드) | x55 | 0 |
| 국민톡톡 (카드) | x56 | 214,360 |
| 차량대금(부채) | x59 | 10,000,000 |

---

## 후잉 DB 유용한 쿼리

### 월별 순지출 (캐시백 차감 반영)
```sql
SELECT
    (entry_date::bigint / 100) AS year_month,
    SUM(money)                  AS 순지출합계,
    SUM(CASE WHEN money > 0 THEN money ELSE 0 END) AS 총지출,
    SUM(CASE WHEN money < 0 THEN money ELSE 0 END) AS 캐시백,
    COUNT(*)                    AS 건수
FROM whooing.entries
WHERE section_id = 's152045'
  AND l_account  = 'expenses'
  AND item       != '잔액조정'
GROUP BY 1
ORDER BY 1;
```

> `SUM(money)` 사용 필수 — `SUM(CASE WHEN money > 0)` 사용 시 캐시백 미차감으로 과다 집계됨
